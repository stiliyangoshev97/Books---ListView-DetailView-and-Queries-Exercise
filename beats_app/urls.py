from django.urls import path

from beats_app import views

from django.conf.urls import url

app_name = 'beats'

urlpatterns = [

    #path('labels/', views.LabelListView.as_view(), name = 'labels'),
    #path('beats/<label>', views.LabelBeatListView.as_view(), name = 'beats'),
    #path('create_label/', views.LabelCreateView.as_view(), name = 'create_label'),
    
]